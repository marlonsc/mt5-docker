# MT5 Docker - Version Configuration (PRIMARY SOURCE OF TRUTH)
# =============================================================================
# All version definitions should reference this file.
# Used by: Dockerfile, docker-compose.yaml, setup.sh
#
# Update these values to trigger new downloads on rebuild.
# =============================================================================

# Python version for Wine (Windows) - 3.12.x for numpy 1.26.4 compatibility
PYTHON_VERSION=3.12.8

# Wine Mono version for .NET support (replaces Gecko in Alpine build)
WINE_MONO_VERSION=9.4.0

# Python packages - versions used in Wine environment
# gRPC 1.76.0: Required by mt5_pb2_grpc.py (GRPC_GENERATED_VERSION)
GRPCIO_VERSION=1.76.0
NUMPY_VERSION=1.26.4

# MetaTrader5 Python package version from PyPI
# Check latest: curl -s https://pypi.org/pypi/MetaTrader5/json | jq -r '.info.version'
MT5_PYPI_VERSION=5.0.5430
