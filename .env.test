# Test container settings (avoid port conflicts with production)
# Credentials are read from .env file (do NOT duplicate here)

MT5_CONTAINER_NAME=mt5docker-test
MT5_RPYC_PORT=48812
MT5_HEALTH_PORT=48002
MT5_VNC_PORT=43000
MT5_VOLUME_NAME=mt5docker-test-data
MT5_NETWORK_NAME=mt5docker-test-network
