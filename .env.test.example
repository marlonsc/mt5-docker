# .env.test - Test container configuration
#
# Copy to .env.test and fill in your credentials:
#   cp .env.test.example .env.test
#
# Port allocation (to avoid conflicts with other containers):
#   - Production:      mt5,             ports 3000, 8001, 8002
#   - neptor tests:    neptor-mt5-test, ports 13000, 18812, 18002
#   - mt5linux tests:  mt5linux-test,   ports 23000, 28812, 28002
#   - mt5docker tests: mt5docker-test,  ports 43000, 48812, 48002

# Container identification
MT5_CONTAINER_NAME=mt5docker-test
MT5_VOLUME_NAME=mt5docker_test_config
MT5_NETWORK_NAME=mt5docker-test-network

# Port mappings (host:container)
MT5_VNC_PORT=43000
MT5_RPYC_PORT=48812
MT5_HEALTH_PORT=48002

# MT5 Credentials (required for trading tests)
# Create a free demo account at: https://www.metaquotes.net/en/metatrader5
MT5_LOGIN=your_login_number
MT5_PASSWORD=your_password
MT5_SERVER=MetaQuotes-Demo

# Health monitoring
HEALTH_CHECK_INTERVAL=30
AUTO_RECOVERY_ENABLED=1
